int v()
{
  int result;
  char input[520];

  fgets(input, 512, stdin);
  printf(input);
  result = m;
  if ( m == 64 )
  {
    fwrite("Wait what?!\n", 1, 12, stdout);
    return system("/bin/sh");
  }
  return result;
}

int main(int argc, const char **argv, const char **envp)
{
  return v();
}